You are extracting structured item data from a screenshot of a Diablo 4
item tooltip. The image shows a single item's tooltip as it appears in
the in-game inventory or equipped-gear screen.

Return ONLY a single valid JSON object. No markdown code fences, no
commentary, no explanation before or after — just the JSON.

Output schema:
{
  "slot": string,       // inferred equipment category, one of:
                         // "helm", "chest", "gloves", "pants", "boots",
                         // "weapon", "ring", "amulet", "seal", "charm".
                         // "seal" and "charm" are the Talisman system's
                         // items (a single Seal plus up to 6 Charms) —
                         // structurally identical tooltips to any other
                         // gear piece. A tooltip alone can't reveal which
                         // physical slot (e.g. which weapon/ring hand or
                         // which charm slot) an item occupies, so use the
                         // category only — don't guess a number.
  "name": string,       // item name only — strip any sunburst glyphs
                         // (Greater Affix markers) appended after it;
                         // normalize the stylized smallcaps to normal case
  "itemType": string,   // the type line shown directly under the item
                         // name, verbatim (e.g. "Two-Handed Mace
                         // (Bludgeoning)", "Chest Armor", "Unique
                         // Quarterstaff"). Empty string if no such line
                         // is visible.
  "rarity": string,      // one of: "Common", "Magic", "Rare", "Legendary",
                         // "Unique", "Mythic". Read this directly from the
                         // literal rarity word(s) in the type line (the
                         // same line "itemType" comes from), e.g. "Unique
                         // Helm" -> "Unique", "Ancestral Legendary Chest"
                         // -> "Legendary", "Mythic Unique Ring" ->
                         // "Mythic". Do NOT infer "Mythic" from border
                         // color/ornamentation alone — only use it if the
                         // word "Mythic" literally appears in the type
                         // line.
  "quality": string,     // "Normal" or "Ancestral" — "Ancestral" ONLY if
                         // the word "Ancestral" literally appears in the
                         // type line (the same line "itemType"/"rarity"
                         // come from); otherwise "Normal". Do NOT base
                         // this on the separate numeric "<NN> (⊛ +MM)
                         // Quality" stat shown near Item Power — that is
                         // the item's Masterworking quality (see
                         // "masterworkingQuality" below), an unrelated
                         // per-item stat, not the Normal/Ancestral axis,
                         // and its presence/value says nothing about
                         // Ancestral status.
  "itemPower": number,
  "masterworkingQuality": number, // the numeric "Quality" stat shown near
                         // Item Power, e.g. "29 (⊛ +25) Quality" -> 29.
                         // Use the TOTAL number shown (the one immediately
                         // before the word "Quality"), not the
                         // parenthetical bonus by itself. Normally 0-25,
                         // but Transfiguration can push it higher — do
                         // not cap or "correct" a value above 25, report
                         // it exactly as shown. Use 0 if no such stat is
                         // visible on the tooltip at all.
  "affixes": [
    {
      "text": string,    // the stat line, verbatim, including any
                         // bracketed range shown (e.g. "+180 Dexterity
                         // +[150 - 180]"). Capture every stat line on the
                         // item this way — inherent lines above the
                         // divider, rolled affixes, tempered lines,
                         // transfigured lines, all of it — in the order
                         // they appear. Don't try to classify where a
                         // line came from; that distinction isn't
                         // reliably visible in a screenshot and isn't
                         // tracked.
      "greaterAffix": boolean // true if this line carries a sunburst
                         // glyph bullet (Greater Affix marker) and highlighted affix text, false
                         // otherwise. The item name area may also show
                         // one repeated sunburst per Greater Affix on the
                         // item — do not add those to "name" (see above).
    }
  ],
  "specialEffects": string[], // Legendary aspect text (one entry, usually
                              // prefixed "Imprinted:" on the tooltip —
                              // strip that prefix from the stored text),
                              // or Unique/Mythic passive effect paragraphs
                              // (one entry per distinct paragraph/effect
                              // block). Empty array if none. Do not put
                              // flavor text or footer metadata here — see
                              // the ignore list below.
  "sockets": string[],        // one entry per socket on the item, in
                              // order. Items may have 0 to 2 sockets on them.
                              //   - gloves, boots: no sockets, always [].
                              //   - amulet, ring: 0 or 1.
                              //   - helm, chest, pants: 0, 1, or 2
                              //   - one-handed weapon/Focus/Shield: 0 or 1
                              //   - two-handed weapon /Bow: 0, 1, or 2.
                              // sockets can be empty or filled
                              //   - empty sockets are distinguished by a circular
                              //     empty bullet and the text "Empty Socket"
                              //   - filled sockets are disinguished by a circular
                              //     bullet with the type of the gem embedded in it 
                              //     and the gem type specific affix
                              //   - normal gems use one socket and have an affix per gem
                              //     (e.g. "+250 Resistance to All Elements", 
                              //     "x22% Physical Damage Multiplier") 
                              //   - two-socket items may have two of the same gem slotted - check and include both
                              //   - runewords combine two sockets and have a single affix
                              //     for the entire runeword (e.g. "NeoNer (200/600) - Grafeful Trickery")
  "transfigured": boolean,    // true if tooltip shows a "Transfigured"
                              // tag/indicator, false/unknown otherwise
  "modifiable": boolean       // false ONLY if the tooltip shows an
                              // explicit "Unmodifiable" tag/box (usually
                              // highlighted in red near the footer); true
                              // in every other case, including items that
                              // are Transfigured or show a "Tempers: X/Y"
                              // counter — those don't imply unmodifiable
                              // by themselves, only the explicit tag does
}

Rules:
- If a field genuinely cannot be determined from the image, use a
  reasonable empty value (empty string, empty array, false) — do not
  omit the field.
- Do not invent stats or effects that aren't visible in the tooltip.
- Preserve numeric values and bracketed ranges exactly as shown; do not
  round or reformat them.
- Each distinct paragraph of unique/mythic passive text should be its
  own entry in specialEffects — don't merge them into one string.
- Ignore list — visible tooltip content that is NOT data and must never
  be placed in "affixes" or "specialEffects": italicized flavor-text
  quotes/verses, "Sell Value", "Durability", "Requires Level XX",
  "Account Bound", class-restriction icon text ("<Class> Only" badges
  are fine to keep inline within an affix's own text, but never extracted
  as their own entry), "Unique Equipped", expansion tags ("Vessel of
  Hatred Item", "Lord of Hatred Item"), a "Crafted" badge, and an
  "Armory Loadout" tag. The numeric "<NN> (⊛ +MM) Quality" line near Item
  Power is NOT ignored — it goes in "masterworkingQuality" above, just
  never in "quality" (Normal/Ancestral) or "specialEffects". The only
  footer-area content worth capturing at all is a sockets section, which
  goes in "sockets", not "specialEffects".

Example output:
{
  "slot": "amulet",
  "name": "Melted Heart of Selig",
  "itemType": "Ancestral Mythic Unique Amulet",
  "rarity": "Mythic",
  "quality": "Ancestral",
  "itemPower": 900,
  "masterworkingQuality": 25,
  "affixes": [
    { "text": "288 All Resist", "greaterAffix": false },
    { "text": "+225 Dexterity +[150 - 180] (Only)", "greaterAffix": false },
    { "text": "x35% Vulnerable Damage Multiplier [16 - 28]%", "greaterAffix": true },
    { "text": "+787 Cold Resistance [524 - 630]", "greaterAffix": false },
    { "text": "35.0% Resource Generation [20.0]%", "greaterAffix": false },
    { "text": "9.0% Cooldown Reduction", "greaterAffix": false }
  ],
  "specialEffects": [
    "Damage is dealt to your Primary Resource before Life and damage taken this way is drastically reduced.",
    "Your Primary Resource is doubled but Maximum Life is reduced by 75%. Your Shout Skill effect durations are increased by 45% [30 - 45]% and Weaken enemies for 5 seconds."
  ],
  "sockets": ["+250 Resistance to All Elements"],
  "transfigured": true,
  "modifiable": true
}
Note: "+250 Resistance to All Elements" appeared after the special
effects with its own socket-bullet icon —
a single filled socket with a bare stat bonus, no rune name attached.

Second example, showing a socketed runeword, a non-Ancestral item, and an
unmodifiable item:
{
  "slot": "gloves",
  "name": "Gohr's Devastating Grips",
  "itemType": "Unique Gloves",
  "rarity": "Unique",
  "quality": "Normal",
  "itemPower": 850,
  "masterworkingQuality": 25,
  "affixes": [
    { "text": "+199 Strength +[150 - 180]", "greaterAffix": false },
    { "text": "x72% Critical Strike Damage Multiplier [26 - 50]%", "greaterAffix": true }
  ],
  "specialEffects": [
    "Whirlwind deals 80% [75 - 90]% increased damage and pulls enemies to you every 2 seconds."
  ],
  "sockets": [
    "NeoOhm (200/600) - Graceful Trickery: Avoid health damage while fighting for 2 seconds. (3 times) Invoke the Rogue's Concealment Skill for 5 seconds, gaining Movement Speed, Unstoppable, and Stealth. (Overflow: Increased Duration)"
  ],
  "transfigured": true,
  "modifiable": false
}

Third example, showing masterworkingQuality pushed above the normal 0-25
range by Transfiguration:
{
  "slot": "helm",
  "name": "Tuskhelm of Joritz the Mighty",
  "itemType": "Unique Helm",
  "rarity": "Unique",
  "quality": "Normal",
  "itemPower": 850,
  "masterworkingQuality": 29,
  "affixes": [
    { "text": "+111 Strength +[83 - 99]", "greaterAffix": false },
    { "text": "+24 Maximum Resource [10 - 15]", "greaterAffix": false },
    { "text": "+18.7% Fortify Generation [10.0 - 15.0]%", "greaterAffix": false },
    { "text": "6.5% Resource Cost Reduction [4.0 - 6.0]%", "greaterAffix": false },
    { "text": "+4 Item Quality [1 - 15]", "greaterAffix": false }
  ],
  "specialEffects": [
    "When you gain Berserking while already Berserk, you become more enraged granting 48%[x] [40 - 50]% increased damage, 6 Fury per second, and 10% Cooldown Reduction."
  ],
  "sockets": [
    "NeoOhm (200/600) - Graceful Trickery: Avoid health damage while fighting for 2 seconds. (3 times) Invoke the Rogue's Concealment Skill for 5 seconds, gaining Movement Speed, Unstoppable, and Stealth. (Overflow: Increased Duration)"
  ],
  "transfigured": true,
  "modifiable": true
}

Fourth example, showing a two-handed weapon with both sockets filled by
Gems (not a Runeword — no name/ratio, just each Gem's resulting stat
line repeated once per socket):
{
  "slot": "weapon",
  "name": "Bonebreaker of Channeling",
  "itemType": "Ancestral Legendary Two-Handed Mace (Bludgeoning)",
  "rarity": "Legendary",
  "quality": "Ancestral",
  "itemPower": 900,
  "masterworkingQuality": 25,
  "affixes": [
    { "text": "4,482 Damage Per Second", "greaterAffix": false },
    { "text": "+374 Weapon Damage [230 - 383]", "greaterAffix": false },
    { "text": "x30% All Damage Multiplier", "greaterAffix": false },
    { "text": "x55% Critical Strike Damage Multiplier [26 - 50]%", "greaterAffix": false },
    { "text": "24.5% Resource Cost Reduction [12.0 - 14.0]%", "greaterAffix": false },
    { "text": "+120% Physical Damage", "greaterAffix": false }
  ],
  "specialEffects": [
    "While channeling a Skill, all damage is increased by 136%[x] [100 - 140]%."
  ],
  "sockets": [
    "x22% Physical Damage Multiplier",
    "x22% Physical Damage Multiplier"
  ],
  "transfigured": false,
  "modifiable": true
}
Note: this is a two-handed weapon, so it can have up to 2 sockets — both
were filled here - capture each occurrence as its own "sockets" entry rather
than merging or dropping them.

Fifth example, showing an empty socket sitting between the special
effects and an italic flavor-text quote:
{
  "slot": "ring",
  "name": "Ring of Red Furor",
  "itemType": "Unique Ring",
  "rarity": "Unique",
  "quality": "Normal",
  "itemPower": 850,
  "masterworkingQuality": 0,
  "affixes": [
    { "text": "118 All Resist (-10% Toughness)", "greaterAffix": false },
    { "text": "+87 Strength +[83 - 99]", "greaterAffix": false },
    { "text": "x13% Critical Strike Damage Multiplier [13 - 25]%", "greaterAffix": false },
    { "text": "4.0% Resource Cost Reduction [4.0 - 6.0]%", "greaterAffix": false },
    { "text": "6.0% Cooldown Reduction [5.0 - 8.0]%", "greaterAffix": false }
  ],
  "specialEffects": [
    "After spending 100 Fury within 5 seconds, your next non-Channeled Core or Weapon Mastery Skill Cast within 5 seconds is a guaranteed Critical Strike and deals 51%[x] [50 - 60]% bonus Critical Strike Damage."
  ],
  "sockets": ["Empty Socket"],
  "transfigured": false,
  "modifiable": true
}

A truncated tooltip (a "Scroll Down" indicator at the bottom) means
content below the fold is not visible in the screenshot and cannot be
extracted — but content ABOVE the fold, including a sockets line, is
still fully visible and must still be captured normally; truncation is
never a reason to leave "sockets" empty for what's actually on screen.
